[
  # MANAGED BY: eclair — system-wide packages (do NOT edit manually; use ./scripts/eclair.sh)
  # hosts/default/user-packages.nix
  # Purpose: central list of system packages managed by `eclair`.
  # Format: a Nix list with one entry per line. `eclair install <pkg>` will
  # insert the package above the marker line below.

  # example packages (use strings)
  "wget"
  "git"
  "docker"
  # ---------------------------
]

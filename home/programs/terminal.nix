{ config, pkgs, ... }:

{
  # Home-module: home/programs/terminal.nix
  # Purpose: Configure terminal emulator(s) at the user level (kitty, alacritty).
  # How to use: enable `programs.kitty` or add terminal packages to `home.packages`.
  # Example:
  #
  # programs.kitty.enable = true;
  # home.packages = with pkgs; [ alacritty kitty ];

  # This file is a documented stub for user terminal configuration.
}

{ config, pkgs, ... }:

{
  # Home-module: home/programs/editors.nix
  # Purpose: Editor configs and example dotfiles (moved from `home.nix`).

  # Example dotfile (uncomment and edit to enable)
  # home.file.".config/alacritty/alacritty.yml".text = ''
  #   sua config aqui
  # '';
}

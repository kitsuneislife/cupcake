// panel.qml — small panel fragment used by main.qml in real configs
// This is an example; real QuickShell configs will include interaction logic.
import QtQuick 2.15

Item {
    width: parent.width
    height: 40

    Rectangle {
        anchors.fill: parent
        color: "transparent"
    }
}
